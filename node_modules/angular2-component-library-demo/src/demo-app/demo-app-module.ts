import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { RouterModule }   from '@angular/router';
import { DemoApp, HomeComponent } from './demo-app/demo-app';
import { HelloWorld } from '../lib/hello-world/HelloWorld';
//import { MedrioModule } from '../../dist/@medrio/components';
import { MedrioModule } from '../lib/';

@NgModule({
  imports: [
        BrowserModule,
        MedrioModule.forRoot(),
        RouterModule.forRoot([
            {
                  path: '',
                  component: HomeComponent
            }

      ])
  ],
  declarations: [
        DemoApp,
        HomeComponent
  ],
  providers: [],
  bootstrap: [ DemoApp ]
})

    
export class DemoAppModule { }
